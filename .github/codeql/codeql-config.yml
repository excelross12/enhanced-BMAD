name: "CodeQL Config"

queries:
  - uses: security-extended
  - uses: security-and-quality

paths-ignore:
  - test/
  - docs/
  - website/

paths:
  - src/
  - tools/

query-filters:
  - exclude:
      id: js/unused-local-variable
